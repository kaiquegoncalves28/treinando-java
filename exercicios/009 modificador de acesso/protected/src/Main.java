import Empresa.Dados.*;
public class Main {
    public static void main(String[] args) {
        TestPackage tp = new TestPackage();
        //protected: Visível para classes do mesmo pacote e para subclasses via herança (mesmo em outros pacotes).
    }
}
